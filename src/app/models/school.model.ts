//Acrescentar Todos os parâmetros do json.
export class SchoolModel{

    constructor(
        code: number,
        name: string,
        latitude: string,
        longitude: string,
        type: string,
        country: string){}
}