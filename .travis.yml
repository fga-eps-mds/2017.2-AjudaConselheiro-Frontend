#.travis.yml

language: node_js

node_js:
  - "8.2.1"

branches:
  only:
  - gh-pages
  - /.*/

before_script:
  - set -e
  - npm install -g @angular/cli
  - npm install

script:
  - ng build --prod

notifications:
  email:
    on_failure: change
    on_success: change
  webhooks: https://fathomless-fjord-24024.herokuapp.com/notify
